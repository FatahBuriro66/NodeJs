const { todo: Todo } = require('../models/index.js')

const createTodoCategory = async (data) => {
    try {
        // const createTodo
    } catch (error) {

    }
}
module.exports = {
    createTodoCategory
}